#pragma once

#include <cstdint>

// #include <lib_private.h> // IMPORTANT: Don't put private headers in public exposed header files

void print_hello();
uint32_t add_numbers(uint16_t x, uint16_t y);
bool isPositive(int32_t num);